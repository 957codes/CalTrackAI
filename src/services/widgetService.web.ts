import { DailyLog } from "../types";

// Web stub — iOS widgets not available on web

export async function refreshWidget(): Promise<void> {}

export async function syncWidgetData(_log: DailyLog): Promise<void> {}
